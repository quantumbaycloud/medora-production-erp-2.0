# ERP Catalog / Master Data

Catalog options are pharmacy-scoped. Medorax Admin controls synchronized options such as
`supplier_category`, `medicine_category`, `payment_term`, `customer_type`, `dosage_form`, and `unit`.
The ERP stores a local cache so supplier and other master-data forms remain usable when the
central Admin is temporarily unavailable.
