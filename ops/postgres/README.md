# PostgreSQL production notes

Use managed PostgreSQL where possible. Enable encrypted connections, automated backups, PITR, least-privilege roles,
connection pooling, and a separate migration role. Never commit production credentials.
