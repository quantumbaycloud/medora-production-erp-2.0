import { ArrowUpRight } from "lucide-react";
import { purchaseData } from "../../../data/purchases/data";
const styles={delivered:"bg-secondary-container text-on-secondary-fixed-variant",processing:"bg-tertiary-fixed text-on-tertiary-fixed-variant",shipped:"bg-primary-fixed text-primary",pending:"bg-surface-container text-on-surface-variant"};
export default function SupplierPurchaseOrders({supplier}){const rows=purchaseData.filter(p=>!supplier?.id||p.supplier_id===supplier.id||p.supplierId===supplier.id);return <div className="overflow-hidden rounded-2xl border border-outline-variant bg-surface-container-lowest shadow-sm"><div className="border-b px-6 py-5"><h2 className="text-xl font-bold">Purchase Orders</h2><p className="mt-0.5 text-sm text-on-surface-variant">Orders linked to this supplier from the ERP purchase service.</p></div><div className="overflow-x-auto"><table className="min-w-full"><thead className="bg-surface-container-low text-xs text-on-surface-variant"><tr><th className="px-6 py-3 text-left">Reference</th><th className="px-6 py-3 text-left">Date</th><th className="px-6 py-3 text-right">Amount</th><th className="px-6 py-3 text-center">Status</th><th className="px-6 py-3"/></tr></thead><tbody>{rows.length?rows.map(o=>{const status=String(o.status||"pending").toLowerCase();return <tr key={o.id} className="border-t border-outline-variant"><td className="px-6 py-4 font-mono text-xs font-semibold text-primary">{o.invoice_number||o.id}</td><td className="px-6 py-4 text-sm">{o.invoice_date?new Date(o.invoice_date).toLocaleDateString("en-IN"):"—"}</td><td className="px-6 py-4 text-right font-mono text-sm">₹{Number(o.total_amount||0).toLocaleString("en-IN")}</td><td className="px-6 py-4 text-center"><span className={`rounded-full px-2.5 py-1 text-xs font-medium ${styles[status]||styles.pending}`}>{o.status||"Pending"}</span></td><td className="px-6 py-4 text-right"><ArrowUpRight size={14} className="ml-auto text-primary"/></td></tr>}) : <tr><td colSpan="5" className="px-6 py-10 text-center text-sm text-outline">No purchase records linked to this supplier.</td></tr>}</tbody></table></div></div>}
